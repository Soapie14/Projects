#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int x = get_int("what's x? ");
    int y = get_int("what's y? ");

    if (x < y)
    {
        printf("x < y");
    }
}

int i = 0;
while (i<3)
{
    print("meow\n");
    i++;
}

for (int i=0; i<3 ; i++)
{
    printf("meow\n")
}

