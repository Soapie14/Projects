#include <stdio.h>
#include <cs50.h>

int main(void)
{
    for (int i=0; i<3; i++);
    {
        printf("meow\n");
    }
}

// do this thing for forever
// needs to be wrapped in function but:
// while (true)
// {
//     printf("meow\n");
// }


// Linex-command line interface(CLI)environment(or terminal)
